function Length(first, second, third){
  let all_lenghts = first.length + second.length + third.length;
  let average_lenghts = Math.floor(all_lenghts / 3);
  console.log(all_lenghts);
  console.log(average_lenghts);
}
